{
  "name": "twitch-guilds",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "migrate": "node --env-file-if-exists=.env src/core/migrate.js",
    "test": "node --env-file-if-exists=.env --test --test-concurrency=1 \"test/*.test.js\""
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "redis": "^6.2.1"
  }
}
